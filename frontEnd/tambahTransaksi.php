<?php
    // session_start();
    // require_once 'conection.php';
    // $koneksi;
    // $con = db_connect();

   

    // $sql = "INSERT INTO orders (order_id, cust_id, emp_id) values ()";
    // $query = mysqli_query($con, $sql);

    // menambahkan id terakhir
    // $id_transaksi = mysqli_insert_id($koneksi);

    // foreach($_SESSION["cart"] as $cart => $val){
    //     $script = "INSERT INTO order_detail (order_id, menu_id, jumlah, order_date, Total_price) VALUES (,".$val["jumlah"].",)";
    //     $hasil = mysqli_query($con, $script);
    // }

    // header("Location: cart.php");
?>